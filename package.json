{
  "name": "etemadmelal-web",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/ensure-assets.mjs && node scripts/check-articles.mjs",
    "build": "astro build",
    "postbuild": "node scripts/postbuild-hint.mjs",
    "preview": "astro preview",
    "build:preview": "npm run build && npm run preview",
    "export:logo": "node scripts/export-logo-full.mjs",
    "download:images": "node scripts/download-images.mjs",
    "check:assets": "node scripts/check-assets.mjs",
    "check:articles": "node scripts/check-articles.mjs",
    "check": "astro check",
    "lint": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.7",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@resvg/resvg-js": "^2.6.2",
    "typescript": "^6.0.3"
  }
}

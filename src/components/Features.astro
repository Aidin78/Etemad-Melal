---
import SectionHeader from './SectionHeader.astro';
import ChartDecoration from './ChartDecoration.astro';
import { siteImages } from '../config/images';
import { featureStepIcons } from '../lib/section-icons';
import type { Locale } from '../i18n/types';
import { getTranslations } from '../i18n';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = getTranslations(locale);
---

<section id="features" class="section-spacing relative overflow-hidden">
  <div class="chart-pattern absolute inset-0 opacity-30" aria-hidden="true"></div>
  <div class="relative mx-auto max-w-6xl px-4 sm:px-6">
    <div class="grid items-center gap-8 lg:grid-cols-2 lg:gap-12">
      <div>
        <SectionHeader
          badge={t.features.badge}
          title={t.features.title}
          titleAccent={t.features.titleAccent}
          subtitle={t.features.subtitle}
        />
        <ChartDecoration class="mt-8 h-16 w-full max-w-xs" />
      </div>
      <div class="relative overflow-hidden rounded-2xl ring-1 ring-accent-gold/20">
        <img src={siteImages.features} alt={t.features.imageAlt} class="aspect-[4/3] w-full object-cover" loading="lazy" />
        <div class="absolute inset-0 bg-image-overlay-top"></div>
        {
          t.features.chartLabel && (
            <div class="absolute bottom-4 right-4 left-4 investment-card rounded-xl p-4">
              <div class="flex items-center justify-between text-sm">
                <span class="text-muted">{t.features.chartLabel}</span>
                <span class="font-bold text-accent">{t.features.chartValue}</span>
              </div>
              <div class="mt-2 h-1.5 overflow-hidden rounded-full bg-[var(--surface-muted)]">
                <div class="h-full w-3/4 rounded-full bg-gradient-to-l from-accent to-accent-gold"></div>
              </div>
            </div>
          )
        }
      </div>
    </div>

    <ul class="mt-10 grid gap-5 sm:mt-16 sm:grid-cols-2 lg:grid-cols-4 sm:gap-6">
      {
        t.features.steps.map((step, i) => (
          <li>
            <article
              class="investment-card group relative overflow-hidden rounded-2xl p-6"
              style={`animation-delay: ${i * 0.1}s`}
            >
              <div class="absolute top-0 left-0 h-1 w-full bg-gradient-to-l from-transparent via-accent-gold to-transparent opacity-0 transition-opacity duration-500 ease-out group-hover:opacity-100" />
              <div class="mb-4 flex items-center justify-between">
                <span class="text-3xl font-bold text-accent-gold/30">{step.num}</span>
                <div class="flex h-12 w-12 items-center justify-center rounded-xl bg-gradient-to-br from-accent-gold/25 to-accent/20 text-accent-gold-light transition-colors duration-500 ease-out group-hover:from-accent-gold/35 group-hover:to-accent/30">
                  <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d={featureStepIcons[i] ?? featureStepIcons[0]} />
                  </svg>
                </div>
              </div>
              <h3 class="text-lg font-bold text-heading">{step.title}</h3>
              <p class="mt-2 text-sm leading-relaxed text-muted">{step.desc}</p>
            </article>
          </li>
        ))
      }
    </ul>
  </div>
</section>

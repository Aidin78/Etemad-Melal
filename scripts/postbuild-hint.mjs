console.log(`
Build complete — output is in dist/

To view locally (styles & images need a web server):
  npm run preview
  then open http://localhost:4321

Do NOT double-click dist/index.html — file:// breaks /_astro/ and /images/ paths.

For cPanel: upload everything INSIDE dist/ into public_html (not the dist folder itself).
`);
